{
  "name": "web-spider",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "dev:desktop": "tauri dev",
    "dev:mobile": "tauri dev --mobile",
    "dev:ios": "tauri ios dev",
    "dev:android": "tauri android dev",
    "build:desktop": "tauri build",
    "build:desktop:debug": "tauri build --debug",
    "build:ios": "tauri ios build",
    "build:android": "tauri android build",
    "build:ios:release": "tauri ios build --release",
    "build:android:release": "tauri android build --release",
    "clean": "cargo clean --manifest-path=src-tauri/Cargo.toml && rm -rf node_modules/.vite && rm -rf src-tauri/gen",
    "clean:all": "cargo clean --manifest-path=src-tauri/Cargo.toml && rm -rf node_modules && rm -rf src-tauri/target && rm -rf src-tauri/Cargo.lock && rm -rf src-tauri/gen",
    "lint": "vue-tsc --noEmit"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.1",
    "@tauri-apps/plugin-dialog": "^2.6.0",
    "@tauri-apps/plugin-opener": "^2.5.3",
    "artplayer": "^5.3.0",
    "hls.js": "^1.6.15",
    "qrcode": "^1.5.4",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.2.0",
    "@tailwindcss/vite": "^4.2.0",
    "@tauri-apps/cli": "^2.10.0",
    "@types/qrcode": "^1.5.6",
    "@vitejs/plugin-vue": "^5.2.1",
    "autoprefixer": "^10.4.24",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.2.0",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "vue-tsc": "^2.1.10"
  }
}
